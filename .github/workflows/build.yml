name: Auto Validate Package and Release

on:
  workflow_dispatch:
    ~
  push:
    branches: [master]
    paths:
      - "src/**"
  pull_request:
    branches: [master]
    paths:
      - "src/**"

permissions:
  contents: write
  pull-requests: write

jobs:
  json-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: json-validate
        id: json-validate
        uses: GrantBirki/json-yaml-validate@v3.3.2 # replace with the latest version
        with:
          comment: "true"
          base_dir: "src"
          exclude_file: "exclude.txt"

  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create ZIP archive with build number
        run: |
          cd src
          zip -r "../KumoOTagayasu-build.mrpack" ./*

      - name: Delete existing release assets
        uses: mknejp/delete-release-assets@v1
        with:
          tag: “build”
          assets: |
            KumoOTagayasu-build.mrpack
            Server-KumoOTagayasu-build.zip
          token: ${{ secrets.GITHUB_TOKEN }} # Required for authentication

      - name: Upload .mrpack file to release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "build"
          files: "KumoOTagayasu-build.mrpack"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build server
        run: |
          mkdir server
          mv "KumoOTagayasu-build.mrpack" ./server
          cd server
          wget https://maven.minecraftforge.net/net/minecraftforge/forge/1.20.1-47.4.6/forge-1.20.1-47.4.6-installer.jar
          java -jar forge-1.20.1-47.4.6-installer.jar --installServer
          wget https://github.com/nothub/mrpack-install/releases/download/v0.16.10/mrpack-install-linux
          chmod 755 mrpack-install-linux
          ./mrpack-install-linux "KumoOTagayasu-build.mrpack" --server-dir ./ --server-file ./forge-1.20.1-47.4.6-installer.jar
          rm -f "KumoOTagayasu-build.mrpack"
          rm -f mrpack-install-linux
          rm -f forge-1.20.1-47.4.6-installer.jar
          rm -f install.log
          zip -r "../Server-KumoOTagayasu-build.zip" ./*
          
      - name: Upload server file to release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "build"
          files: "Server-KumoOTagayasu-build.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
